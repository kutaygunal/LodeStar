# WP-G Devops Task (devops-wpg)

You are `devops-wpg`. Project: /c/Users/kutay/Desktop/Projects/Lodestar.
Read PLAN.md and docs/working-rules.md. Commit and push WP-G (Qt UI wiring).

## Steps
1. `git status` to see changes.
2. Stage and commit the WP-G implementation + tests + PLAN.md status update as a `chore(wp-g): ...` commit.
3. If a git remote exists, push. If not, commit locally only (do NOT attempt a bogus push).
4. Report the commit hash.

Do NOT run `find /`. Do NOT modify code.

When done, run: `herdr agent prompt orchestrator 'DONE devops-wpg <commit-hash>'`
